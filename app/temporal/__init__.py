"""Temporal workflow engine integration package.

Subpackages:
    models/      — Dataclass inputs/outputs shared between workflows & activities.
    activities/  — Activity implementations (can perform I/O).
    workflows/   — Workflow definitions (must be deterministic, no I/O).
"""
